Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
# Postgres driver. Unused on local SQLite, required wherever
# JATTA_DATABASE_URL points at Supabase. psycopg 3 rather than psycopg2: it
# ships wheels for every Python version this runs on, including the 3.12
# default on Vercel, so no deployment needs a compiler.
psycopg[binary]==3.2.13
